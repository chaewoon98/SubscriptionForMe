<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.subscriptionforme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".collection.ChildActivity" />

        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".home.AddSubscriptionActivity" />

        <activity android:name=".recommendation.DetailRecommendationActivity" />

        <!--   설정 액티비티 추가     -->
        <activity android:name=".setting.SettingActivity"/>
        <!--   카드 선택 엑티비티 추가    -->
        <activity android:name=".setting.card.CardActivity"/>
        <!--   약관 확인 엑티비티 추가     -->
        <activity android:name=".setting.card.AgreementActivity"/>
        <!--    CSV 추출하기    -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.subscriptionforme.setting.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths"/>
        </provider>

    </application>


</manifest>